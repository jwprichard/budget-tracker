# PostgreSQL Configuration
POSTGRES_USER=budget_user
POSTGRES_PASSWORD=budget_password
POSTGRES_DB=budget_tracker

# Port Mappings
POSTGRES_PORT=5432
BACKEND_PORT=3000
FRONTEND_PORT=5173

# Akahu Configuration (Bank Synchronization)
AKAHU_BASE_URL=https://api.akahu.io
AKAHU_APP_TOKEN=your_personal_app_token_here

# Encryption (for secure token storage)
# Generate a key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_64_character_hex_encryption_key_here
